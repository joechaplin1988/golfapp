# Scraper outputs — regenerated every run, not source
tee_times_*.json
run_*.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Never commit secrets. DATABASE_URL lives in GitHub Actions secrets / n8n env,
# never in the repo.
.env

# Netlify writes this at deploy time from the SUPABASE_ANON_KEY env var
web/config.js

# Discovery pipeline outputs — regenerable, not source
candidates_kent.json
candidates_sussex.json
kent_sussex_osm_golf.json
kent_directory_scan.json

# discovery caches + working files
# (candidates_review_*.csv and recheck_*.csv are NOT ignored: they are the
#  evidence behind coverage.csv, which is the reviewable list of every club)
osm_*.json
union_*.json
approved_rows.csv
approved_rows_*.csv
candidates_recovered.json
course_profiles.csv.bak

# Fingerprint/probe run logs — noise, the review CSVs are the evidence
fp_*.log
probe_*.log
rc_*.log
